{
  "schemaVersion": 2,
  "services": [
    {
      "name": "newt",
      "image": "fosrl/newt",
      "environment": [
        { "key": "PANGOLIN_ENDPOINT", "value": "${PANGOLIN_ENDPOINT}" },
        { "key": "NEWT_ID", "value": "${NEWT_ID}" },
        { "key": "NEWT_SECRET", "value": "${NEWT_SECRET}" }
      ]
    }
  ]
}
